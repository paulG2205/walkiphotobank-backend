name: FastAPI CI/CD

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v2

      - name: Set up Python
        uses: actions/setup-python@v2
        with:
          python-version: '3.x'

      - name: Install dependencies
        run: |
          ls -la  # Verificar los archivos del repositorio
          cat requirements.txt  # Asegurarse de que el archivo esté presente
          pip install -r requirements.txt

      - name: Run FastAPI locally (for testing)
        run: |
          uvicorn main:app --host 0.0.0.0 --port 8080

      - name: Upload artifacts
        uses: actions/upload-artifact@v3
        with:
          name: fastapi-artifacts
          path: ./  # Ruta a los artefactos generados
